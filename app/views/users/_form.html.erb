<%= form_with(model: user, local: true) do |form| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :username %>
    <%= form.text_field :username, id: :user_username %>
  </div>

  <div class="field">
    <%= form.label :age %>
    <%= form.number_field :age, id: :user_age %>
  </div>

  <div class="field">
    <%= form.label :wins %>
    <%= form.number_field :wins, id: :user_wins %>
  </div>

  <div class="field">
    <%= form.label :losses %>
    <%= form.number_field :losses, id: :user_losses %>
  </div>

  <div class="field">
    <%= form.label :elo_score %>
    <%= form.number_field :elo_score, id: :user_elo_score %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
